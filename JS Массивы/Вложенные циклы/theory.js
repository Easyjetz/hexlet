// есть функция flat у массивов, которая "выправляет" его, сводит всё к одному массиву

[[3, 2], 5, 3, [3, 4, 2], 10].flat();
// [3, 2, 5, 3, 3, 4, 2, 10]

// в библиотеке lodash есть 3 варианта flatten(flat).