/*
 Инкапсуляция – это объединение функций и данных в рамках одной структуры, внутреннее состояние которой 
 (данные) скрыты от внешнего мира (этот аспект мы разберем позже). Такие функции называют методами.  
*/

// Вызов метода
user.getName();

// Вызов функции
getName(user);

